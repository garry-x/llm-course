# Project Contract

## User Task

[REPLACE: State one user, one task family, and the failure cost.]

## Output and Permission Boundary

[REPLACE: Define allowed output fields, schema/format, citations, tool permissions, and what the system must never execute or claim.]

## Non-goals

[REPLACE: State what this project explicitly does not solve.]

## Baseline

[REPLACE: Name the baseline model, prompt, retrieval path, rule system, or pipeline and its version.]

## Primary Intervention

[REPLACE: State the one primary factor changed from the baseline. List any unavoidable confound explicitly.]

## Success Metrics and Evaluation Boundary

[REPLACE: Define quality, safety, latency, and cost metrics; identify the frozen evaluation set and what the metrics do not prove.]

## Reject / Rollback Rule

[REPLACE: State the predeclared condition that blocks release, triggers rollback, or hands work to a human.]

## Resource Envelope

[REPLACE: State model/runtime, maximum input/output tokens, concurrency or load shape, and resource/SLO budget.]
