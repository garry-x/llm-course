# Worked Example Pack


本包把章节正文、讨论课、书面题和编程作业中的核心概念落成可复算例子。它的作用是帮助学生把公式、shape、代码和常见错误连接起来；配套题目见 [书面推导与概念题题库](written-problem-set.md)。

## Core Worked Examples

| example_id | chapter | learning target | inputs and shapes | worked trace | common failure | learning link | scope note |
| --- | --- | --- | --- | --- | --- | --- | --- |
| WE-CH01-BPE | Ch01 Tokenization / BPE | Trace BPE merge steps, token savings, and compression ratio. | Text `abababa`, `vocab_size=258`, initial byte ids `[97,98,97,98,97,98,97]`. | Step 0 chooses `(a,b)` with count 3, creates token `256="ab"`, applies non-overlapping replacements, and changes length `7->4`; step 1 chooses `(256,256)` with count 2, creates `257="abab"`, changes length `4->3`; final ids are `[257,256,97]` and compression ratio is `3/7`. | Reusing the original pair counts after a merge or allowing overlapping replacements; diagnostic is first-step savings not 3 or final ids not matching the non-overlap merge. | `assignments/ch01_bpe/`, `bpe_training_trace`, `written-problem-set.md` Ch01 | stable algorithmic procedure |
| WE-CH01-REPORT | Ch01 Tokenization / Evaluation | Compute tokenizer token counts, round-trip success, and embedding parameter budget. | Texts `hello hello`, `世界`, `emoji 😊`; vocab size `280`, `d_model=16`. | Encode each text, average token lengths, take P95 by sorted nearest rank, verify decode(encode(x)) equals x, and compute `280*16` embedding params. | Claiming a tokenizer is good because it round-trips only; diagnostic is no token-cost or embedding-budget comparison. | `assignments/ch01_bpe/`, written Ch01 tokenizer experiment | stable tokenizer metric |
| WE-CH01-GROUPS | Ch01 Tokenization / Evaluation | Compare token cost across language/domain groups. | Groups `english`, `cjk`, `code`; each has two short texts. | Run the same tokenizer report per group, compare `tokens_per_character`, identify max/min groups, and compute disparity as `max_rate/min_rate`. | Reporting one global average that hides the expensive group; diagnostic is no per-language or per-domain token cost. | `assignments/ch01_bpe/`, written Ch01 tokenizer experiment | grouped token-cost metric |
| WE-CH02-LOOKUP | Ch02 Embedding / Lookup | Show that embedding lookup equals one-hot matrix multiplication. | Token ids `[[0,2],[1,0]]`; embedding matrix `E` shape `[3,2]`. | `one_hot(token_ids)` has shape `[2,2,3]`; multiplying by `E` gives `[2,2,2]` and matches `E[token_ids]`. | Treating embedding lookup as a nonlinear layer with extra parameters; diagnostic is wrong output shape or a second learned matrix. | `assignments/ch02_embeddings/`, written Ch02 lookup problem | stable embedding foundation |
| WE-CH02-TIEDHEAD | Ch02 Embedding / LM Head | Connect lookup, embedding scaling, parameter count, and tied output logits. | `V=3`, `d=2`, token ids `[[0,2],[1,0]]`, hidden state `h=[1,1]`, embedding matrix `E`. | Lookup returns `[B,T,d]`; the chapter's embedding scaling multiplies values by `sqrt(d)` without changing parameter count; tied LM head computes logits as `h E^T`. | Counting tied input/output matrices twice or confusing embedding lookup with the final vocabulary projection; diagnostic is wrong `V*d` budget or logits shape. | `assignments/ch02_embeddings/`, Ch06 LM head bridge | embedding-to-logits bridge |
| WE-CH02-SGNS | Ch02 Embedding / Word Vectors | Build a co-occurrence matrix and compute skip-gram negative sampling loss. | Token ids `[0,1,2,1]`, window size `1`, center/positive/negative vectors. | Window counts directed center-context pairs; SGNS uses `-logsigmoid(pos_dot) - sum(logsigmoid(-neg_dot))`. | Treating negative samples as extra positive contexts; diagnostic is using `logsigmoid(neg_dot)` instead of `logsigmoid(-neg_dot)`. | `assignments/ch02_embeddings/`, written Ch02 SGNS problem | stable word-vector objective |
| WE-CH02-SGNSGRAD | Ch02 Embedding / Word Vectors / Tensor Derivatives | Compute the SGNS center-vector gradient and interpret the update direction. | One center vector, one positive context vector, one negative vector, dot products near zero. | `dL/dv_c=(sigma(u_o^T v_c)-1)u_o + sum_k sigma(u_k^T v_c)u_k`, divided by batch size under mean reduction; gradient descent moves toward positives and away from negatives. | Getting the sign backward for negative samples or forgetting mean-reduction scaling; diagnostic is autograd mismatch on a one-example batch. | `assignments/ch02_embeddings/`, written Ch02 SGNS gradient problem | word-vector training dynamics |
| WE-CH02-SPMI | Ch02 Embedding / Word Vectors | Compute PMI and shifted PMI from a co-occurrence table. | Counts `X_ij=4`, row total `X_i*=10`, column total `X_*j=8`, total `X_**=100`, negative samples `K=5`. | `PMI=log(4*100/(10*8))=log(5)`; shifted PMI is `log(5)-log(5)=0`; zero-count pairs have undefined PMI rather than value 0. | Explaining SGNS purely as a classifier without the implicit matrix-factorization view; diagnostic is no connection between dot products and co-occurrence statistics. | `assignments/ch02_embeddings/`, written Ch02 SGNS/PMI problem | stable distributional-statistics derivation |
| WE-CH02-GLOVE | Ch02 Embedding / Word Vectors | Compute one GloVe weighted residual from a nonzero co-occurrence. | Count `X_ij=4`, `x_max=100`, `alpha=0.75`, vectors and biases. | Residual is `w_i^T w_j + b_i + b_j - log X_ij`; weight is `min((X_ij/x_max)^alpha,1)`; loss contribution is weight times residual squared. | Taking `log(0)` for missing pairs or treating all counts equally; diagnostic is no nonzero-count mask or no weighting function. | `assignments/ch02_embeddings/`, written Ch02 GloVe problem | stable global co-occurrence objective |
| WE-CH02-ANALOGY | Ch02 Embedding / Word Vectors | Compute cosine similarity and solve a 3CosAdd analogy. | Vectors for `man=[0,1]`, `king=[1,1]`, `woman=[0,2]`, `queen=[1,2]`, `apple=[-1,-1]`. | Query is `king - man + woman = [1,2]`; after cosine normalization and excluding input words, `queen` ranks first. | Returning one of the query words or using raw dot product without normalization; diagnostic is wrong top result on scale-changed vectors. | `assignments/ch02_embeddings/`, written Ch02 analogy problem | empirical static-vector diagnostic |
| WE-CH02-SINUSOID | Ch02 Embedding / Position Encoding | Compute a small sinusoidal position table. | `d_model=4`, positions `0,1,2`; dimensions are `[sin(pos), cos(pos), sin(pos/100), cos(pos/100)]`. | Position 0 is `[0,1,0,1]`; position 1 is approximately `[0.841,0.540,0.010,1.000]`; high-frequency dimensions change faster than low-frequency dimensions. | Treating sinusoidal encoding as learned parameters or forgetting that even dimensions are sine and odd dimensions are cosine. | `assignments/ch02_embeddings/`, Ch02 sinusoidal encoding written problem | stable fixed-position function |
| WE-CH02-ROPE | Ch02 Embedding / Position Encoding / RoPE | Show that RoPE preserves vector norm while changing relative dot products by position difference. | Two 2D query/key vectors `q=[1,0]`, `k=[0,1]`; positions `m=0`, `n=1`; angle `theta`. | Rotate with `R_m` and `R_n`; show `norm(R_m q)=norm(q)`, `norm(R_n k)=norm(k)`, and `(R_m q)^T(R_n k)` depends on `n-m`. | Treating RoPE as adding a position vector; diagnostic is norm drift or loss of relative-position dependence. | `assignments/ch02_embeddings/`, Ch02 written problem | stable theory with notation boundary |
| WE-CH03-EQUIV | Ch03 Scaled Dot-Product Attention | Verify self-attention permutation equivariance without positions or masks. | Sequence matrix `X` and permutation `P=[2,0,1]`. | Compute `Attention(X)` and `Attention(PX)`; the latter should equal rows of `Attention(X)` permuted by `P`. | Expecting attention to know order without positional signal; diagnostic is assuming content-only self-attention distinguishes swapped sequences. | `assignments/ch03_attention/`, Ch03/Ch02 position written problem | stable structural property |
| WE-CH03-QKV | Ch03 Scaled Dot-Product Attention | Compute Q/K/V projection shapes, parameter count, and score tensor shape. | `B=2`, `T=3`, `d_model=4`, `d_k=2`; three no-bias projection matrices. | Each projection has `4*2` parameters; total QKV params are `24`; Q/K/V shapes are `[2,3,2]`; `QK^T` gives `[2,3,3]`. | Counting batch or sequence length as parameters, or confusing score tensor size with learned weights; diagnostic is parameter count changes when `T` changes. | `assignments/ch03_attention/`, Ch04 MHA bridge | shape and parameter accounting |
| WE-CH03-ATTN | Ch03 Scaled Dot-Product Attention | Compute masked attention weights for a three-token causal sequence. | `Q,K,V` shape `(T=3,d=2)`; mask blocks future columns above the diagonal. | Compute `QK^T/sqrt(2)`, replace masked logits with large negative value, apply row-wise softmax, multiply by `V`. | Applying softmax before masking; diagnostic is nonzero attention mass on future tokens. | `assignments/ch03_attention/`, DER-04, W2 shape drill | stable theory and implementation detail |
| WE-CH03-PADMASK | Ch03 Attention Masks | Combine causal and padding masks for a batch. | Padding mask `[[1,1,0,0],[1,1,1,0]]`; causal mask shape `[4,4]`. | Broadcast causal to `[1,4,4]`, valid keys to `[2,1,4]`, take logical AND; padded key columns and future columns are all blocked. | Masking only padded query rows or applying padding mask after softmax; diagnostic is attention mass on padded key positions. | `assignments/ch03_attention/`, written Ch03 mask problem | batch mask semantics |
| WE-CH03-BWD | Ch03 Attention Backward | Compute softmax Jacobian and the gradient from attention output back to logits. | One query with `p` shape `(T,)`, `V` shape `(T,D)`, upstream gradient `g` shape `(D,)`. | First compute `dL/dp_i=g^T v_i`, then `dL/dz_j=p_j(dL/dp_j-sum_i p_i dL/dp_i)`. | Treating softmax as elementwise independent; diagnostic is gradient rows not summing to zero or mismatch with autograd. | `assignments/ch03_attention/`, written Ch03 softmax problem | stable backward math |
| WE-CH03-QKVBWD | Ch03 Attention Backward / Tensor Derivatives | Propagate attention gradients from output all the way to Q, K, and V. | `Q,K,V,dO` shapes `[B,T,D]`, scores `S=QK^T/sqrt(D)`, attention weights `P=softmax(S)`. | Compute `dV=P^T dO`, `dP=dO V^T`, row-wise `dS=P*(dP-sum(P*dP))`, then `dQ=dS K/sqrt(D)` and `dK=dS^T Q/sqrt(D)`. | Stopping at logits or forgetting the scale factor on `dQ/dK`; diagnostic is mismatch with autograd even when forward attention is correct. | `assignments/ch03_attention/`, Ch03 written tensor-derivative problem | core attention backprop chain |
| WE-CH03-ENTROPY | Ch03 Attention / Complexity | Compute attention entropy and dense score memory. | Weights `[1,0,0]`, `[1/3,1/3,1/3]`; `B=2,H=4,T=8,fp16`. | Entropies are `0` and `log(3)`; score memory is `2*4*8*8*2` bytes. | Treating a sharp heatmap as causal explanation or forgetting the `T^2` score tensor. | `assignments/ch03_attention/`, Ch03 entropy/complexity written problem | diagnostic metric and memory estimate |
| WE-CH04-MHA | Ch04 MHA / GQA / MLA | Show that multi-head attention reorganizes, rather than increases, QKV parameters. | `d_model=8`, `n_heads=2`, so `d_k=4`; compare one 8-dimensional head with two 4-dimensional heads. | Single-head QKV parameters are `3*8*8=192`; two-head MHA parameters are `2*3*8*4=192`; attention score shape changes from `[T,T]` to `[2,T,T]`. | Claiming MHA works because it simply adds more QKV parameters; diagnostic is parameter count not matching the single-head baseline. | `assignments/ch04_multihead/`, Ch04 MHA parameter-equivalence written problem | stable architecture accounting |
| WE-CH04-GQA | Ch04 MHA / GQA / MLA | Compare KV-cache memory for MHA and GQA with the same query heads. | `n_q=8`, `n_kv=2`, `d_head=64`, `T=1024`, fp16 KV cache. | MHA stores `2*T*n_q*d_head*2` bytes; GQA stores `2*T*n_kv*d_head*2` bytes; ratio is `n_kv/n_q=0.25`. | Counting query heads for KV cache in GQA; diagnostic is no memory reduction despite fewer KV heads. | `assignments/ch04_multihead/`, Ch04 KV-cache analysis | implementation detail tied to architecture |
| WE-CH04-BUDGET | Ch04 MHA / GQA / MLA | Compute full KV-cache budget across batch and layers. | `layers=24`, `batch=4`, `T=2048`, `n_heads=32`, `n_kv=8`, `d_head=128`, `d_latent=512`, fp16. | Per token: MHA `8192`, GQA `2048`, MLA `512` elements; total bytes multiply by `layers*batch*T*2`; compression vs MHA is `4x` for GQA and `16x` for MLA. | Reporting only per-token ratios without batch/layer units; diagnostic is memory estimate too small by `batch*layers`. | `assignments/ch04_multihead/`, Ch04 written cache budget | architecture memory accounting |
| WE-CH04-MAP | Ch04 MHA / GQA / MLA | Write the Q-head to KV-head mapping for GQA. | `n_heads=8`, `n_kv_heads=2`. | `n_rep=4`; query heads 0-3 map to KV head 0 and heads 4-7 map to KV head 1: `[0,0,0,0,1,1,1,1]`. | Assuming GQA reduces the number of Q heads; diagnostic is output attention still has 8 query heads. | `assignments/ch04_multihead/`, Ch04 GQA written problem | grouping implementation detail |
| WE-CH04-REPEATKV | Ch04 MHA / GQA / MLA | Repeat grouped K/V heads for query-head attention computation. | K/V shape `[B,2,T,D]`, `n_rep=4`, so query heads are `8`. | Head 0 is copied into output heads 0-3 and head 1 into output heads 4-7; attention computes with 8 query heads while cache stores only 2 K/V heads. | Materializing and caching the repeated K/V tensor; diagnostic is KV cache size incorrectly using query-head count. | `assignments/ch04_multihead/`, Ch04 GQA implementation | grouped-query shape operation |
| WE-CH04-MLAABSORB | Ch04 MHA / GQA / MLA | Verify MLA K-decompression matrix absorption for attention scores. | Query head `q_h` shape `[D]`, latent key `c_s` shape `[L]`, K up-projection `W_UK,h` shape `[D,L]`. | Explicit score uses `q_h^T(W_UK,h c_s)`; absorbed score first computes `q_h W_UK,h` in latent width, then dots with `c_s`; associativity makes the two scores equal before RoPE terms. | Caching decompressed K for every token or assuming RoPE can always commute with the up-projection; diagnostic is score mismatch against explicit decompression. | `assignments/ch04_multihead/`, `mla_absorbed_attention_scores`, Ch04 MLA written problem | latent-attention linear algebra |
| WE-CH05-NORM | Ch05 Transformer Block / Norm / FFN | Distinguish LayerNorm centering from RMSNorm scaling. | Vector `x=[1,2,5]`; compare mean-centered variance against root-mean-square. | LayerNorm subtracts mean then divides by standard deviation; RMSNorm divides by `sqrt(mean(x^2)+eps)` without centering. | Claiming RMSNorm output has zero mean; diagnostic is output mean not equal to zero. | `assignments/ch05_block/`, DER-07, written norm question | stable normalization math |
| WE-CH05-RMSBWD | Ch05 Transformer Block / Norm / Tensor Derivatives | Compute the RMSNorm input gradient and explain the coupling term. | `x,gamma,dY` shape `[D]`; `r=sqrt(mean(x^2)+eps)`. | Let `g=dY*gamma`; then `dX=g/r - x * mean(g*x) / r^3`. The second term appears because every feature contributes to the shared RMS denominator. | Treating RMSNorm as independent elementwise scaling; diagnostic is missing the `mean(g*x)` coupling term and failing autograd comparison. | `assignments/ch05_block/`, Ch05 RMSNorm backward problem | normalization backward chain |
| WE-CH05-PRENORM | Ch05 Transformer Block / Gradient Flow | Compare linearized Pre-Norm and Post-Norm residual gradient factors. | Sublayer slopes `[0.2,-0.1,0.3]`, norm slopes `[0.5,0.5,0.5]`. | Pre-Norm factors are `1 + f' n' = [1.1,0.95,1.15]`; Post-Norm factors are `n'(1+f') = [0.6,0.45,0.65]`, showing why norm after residual can sit directly on the shortcut path. | Treating Pre-Norm as a proof of unlimited depth stability; diagnostic is no caveat about initialization, optimizer, residual scaling, and full Jacobians. | `assignments/ch05_block/`, Ch05 gradient-path written problem | local linearization model |
| WE-CH05-SWIGLU | Ch05 Transformer Block / Norm / FFN | Match SwiGLU hidden width to a 4x GELU FFN parameter budget. | `d_model=24`, GELU hidden width `4*d_model`. | GELU params `2*24*96=4608`; SwiGLU hidden width `2*96/3=64`; SwiGLU params `3*24*64=4608`. | Using 4x width for SwiGLU and tripling the FFN parameter count. | `assignments/ch05_block/`, written SwiGLU budget question | stable FFN parameter accounting |
| WE-CH05-RESOURCE | Ch05 Transformer Block / Resource Estimate | Estimate one block's parameter count, FLOPs, and activation memory. | `B=2`, `T=4`, `d_model=8`, `n_heads=2`, `d_ff=16`, fp16. | MHA params `4d^2`; SwiGLU params `3*d*d_ff`; attention score memory `B*H*T*T*dtype_bytes`; major FLOPs sum QKV, scores, value mixing, output projection, and SwiGLU linears. | Confusing parameter memory with activation memory; diagnostic is no `T^2` term for attention scores. | `assignments/ch05_block/`, Ch05 resource written problem | coarse systems estimate |
| WE-CH05-CKPT | Ch05 Transformer Block / Training Memory | Compute activation checkpointing memory-compute tradeoff. | Activation memory `1000` bytes, forward FLOPs `300`, checkpointed fraction `0.5`. | Save `500` activation bytes; recompute `150` forward FLOPs during backward; baseline training FLOPs `900`, checkpointed FLOPs `1050`, multiplier `1.167`. | Claiming checkpointing reduces FLOPs or parameter memory; diagnostic is lower activation memory but higher compute. | `assignments/ch05_block/`, written Ch05 resource problem | training memory tradeoff |
| WE-CH06-LMSHIFT | Ch06 GPT Assembly / Objective | Compute causal LM loss with label shift. | Tokens `[BOS,a,b,EOS]`; logits shape `[1,4,V]`; ignore index may mask padding labels. | Use logits at positions 0-2 to predict labels `[a,b,EOS]`; position 3 has no next-token target; ignored labels are excluded from the CE mean. | Training logits to predict the same-position token or including padding in the denominator; diagnostic is unrealistically low loss or padding gradients. | `assignments/ch06_gpt/`, written Ch06 label-shift problem | next-token objective |
| WE-CH06-TIEDLOSS | Ch06 GPT Assembly / Objective | Connect tied LM Head logits with causal label shift. | `|V|=4`, `d=2`, input ids `[0,1,2]`, hidden states `H`, embedding matrix `E`. | With tying, logits are `H E^T` with shape `[3,4]`; use positions 0-1 to predict targets `[1,2]`; tying saves `|V|d` output-head parameters but couples input and output token geometry. | Treating tying as an inverse mapping or using the final position logits in the training loss; diagnostic is wrong CE term count or double-counted LM Head params. | `assignments/ch06_gpt/`, Ch02 embedding-to-logits bridge | decoder-only objective bridge |
| WE-CH06-TIEDGRAD | Ch06 GPT Assembly / Tensor Derivatives | Compute tied LM Head CE gradients for hidden states and embedding rows. | Hidden states `H` shape `[N,D]`, tied embedding `E` shape `[V,D]`, targets with one ignored row. | Compute `G=(softmax(H E^T)-one_hot(target))/N_valid`; ignored rows are zero; then `dH=G E` and `dE=G^T H`. Every vocabulary row can receive output-side gradient, not only the target row. | Updating only the correct token row or averaging over ignored labels; diagnostic is mismatch with autograd and wrong loss denominator. | `assignments/ch06_gpt/`, Ch06 tied LM head gradient test | decoder objective backward chain |
| WE-CH06-PARAMS | Ch06 GPT Assembly / MoE | Estimate GPT block parameter count and identify tied embedding effect. | `vocab=1000`, `d_model=128`, `n_layers=2`, FFN width `4*d_model`. | Count token embedding, position embedding, attention projections, FFN matrices, norms, and LM head; if weights are tied, LM head reuses token embedding. | Double-counting tied LM head; diagnostic is parameter estimate larger by `vocab*d_model`. | `assignments/ch06_gpt/`, project capacity planning | stable implementation accounting |
| WE-CH06-MOEBUDGET | Ch06 GPT Assembly / MoE | Compute MoE total versus activated parameters. | `d_model=16`, `expert_hidden=32`, `n_routed=8`, `top_k=2`, `shared=1`, bias-free SwiGLU experts. | Each expert has `3*16*32=1536` params; total experts are 9; activated experts per token are 3; router has `16*8=128`; total params are `9*1536+128`, activated expert params are `3*1536`, and capacity-to-compute ratio is `9/3=3`. | Treating total MoE params as per-token compute; diagnostic is no distinction between capacity and activated compute. | `assignments/ch06_gpt/`, Ch06 MoE written problem | sparse activation accounting |
| WE-CH06-MOELOAD | Ch06 GPT Assembly / MoE | Compute expert overflow and explain why load balance affects throughput. | `B*T=12`, `top_k=2`, expert counts `[12,6,4,2]`, per-expert capacity `8`. | Total routed assignments are `24`; overflows are `[4,0,0,0]`; drop rate is `4/24=16.7%` if excess assignments are dropped. | Claiming MoE is automatically faster because activated parameters are fewer; diagnostic is no capacity, dispatch, or busiest-expert calculation. | `assignments/ch06_gpt/`, Ch06 MoE written problem | sparse routing systems constraint |
| WE-CH06-MOEBALANCE | Ch06 GPT Assembly / MoE | Compute Switch-style router load-balancing loss. | Router probabilities over 3 experts for 4 tokens; top-1 expert ids `[0,0,1,2]`. | Load fractions are `[0.5,0.25,0.25]`; mean router probabilities are `[0.4,0.375,0.225]`; load loss is `3*(0.5*0.4 + 0.25*0.375 + 0.25*0.225)=1.05`. | Looking only at top-k counts or only at router probabilities; diagnostic is no penalty when assignments collapse despite diffuse probabilities. | `assignments/ch06_gpt/`, `moe_load_balance_loss`, Ch06 MoE balance problem | sparse router training diagnostic |
| WE-CH07-DATA | Ch07 Training Loop / Data Quality | Compute n-gram repetition and train/eval overlap rates. | Train tokens `[10,11,12,13,20,21,22]`, eval tokens `[0,10,11,12,1,20,21,22]`, `n=3`. | Eval has 6 trigrams; `(10,11,12)` and `(20,21,22)` overlap with train, so overlap rate is `2/6`. | Treating low val loss as proof of generalization without checking overlap; diagnostic is high overlap rate. | `assignments/ch07_training/`, Ch07 written data problem | coarse data-leakage diagnostic |
| WE-CH07-CURATION | Ch07 Training Loop / Data Curation | Gate whether a dataset mix is ready for training rehearsal. | Web `600k` tokens, duplicate `3%`, quality `92%`; code `400k` tokens, duplicate `2%`, quality `90%`; thresholds require `900k` tokens, duplicate `<=5%`, quality `>=88%`, two domains. | Total tokens are `1M`; weighted duplicate rate is `2.6%`; weighted quality pass rate is `91.2%`; domain shares are `60%/40%`, so size, dedup, quality and mixture gates pass if eval overlap and privacy gates also pass. | Starting a training run because token count is high while duplicate rate, eval contamination, PII, or domain imbalance fail; diagnostic is no data curation gate. | `training_data_curation_report`, Ch07 data pipeline written problem | training-data readiness gate |
| WE-CH07-CEGRAD | Ch07 Training Loop / Tensor Derivatives | Compute CE logits gradient for one token and one ignored token. | Logits produce probabilities `[0.7,0.2,0.1]`; target `0`; another position has `ignore_index`. | Valid-token gradient is `[-0.3,0.2,0.1] / N_valid`; ignored position gradient is all zeros and does not enter the mean denominator. | Averaging over ignored padding/prompt positions; diagnostic is prompt or padding logits receiving gradient. | `assignments/ch07_training/`, Ch07 CE gradient written problem | stable tensor derivative |
| WE-CH07-LABELSMOOTH | Ch07 Training Loop / Objectives | Compute label-smoothed cross entropy for one token. | Vocab size `4`, target `2`, epsilon `0.1`, log probabilities `[-3.0,-2.0,-0.5,-4.0]`. | Target distribution is `[0.0333,0.0333,0.9,0.0333]`; loss is `-sum_i q_i log p_i`. | Treating label smoothing as changing the target id; diagnostic is still using a one-hot target distribution. | `assignments/ch07_training/`, Ch07 objective written problem | stable soft-target objective |
| WE-CH07-ECE | Ch07 Training Loop / Evaluation | Compute expected calibration error from confidence buckets. | Four predictions have confidences `[0.9,0.8,0.7,0.6]` and correctness `[1,1,0,0]`; use two bins `[0.5,0.75)`, `[0.75,1.0]`. | Low bin accuracy is `0/2`, confidence `0.65`; high bin accuracy is `2/2`, confidence `0.85`; ECE is `0.5*0.65 + 0.5*0.15 = 0.40`. | Treating softmax max probability as calibrated confidence; diagnostic is confidence buckets whose empirical accuracy is far lower or higher. | `assignments/ch07_training/`, Ch07 calibration written problem | stable probability-calibration metric |
| WE-CH07-CLIP | Ch07 Training Loop / Stability | Compute global gradient clipping. | Two gradient tensors `[3,0]` and `[4]`, max norm `2`. | Global norm is `5`; clip coefficient is `2/5`; clipped gradients are `[1.2,0]` and `[1.6]`. | Clipping each tensor independently; diagnostic is layer-to-layer gradient ratios change unnecessarily. | `assignments/ch07_training/`, Ch07 stability written problem | stable optimization safeguard |
| WE-CH07-ADAMW | Ch07 Training Loop | Execute one AdamW update and separate gradient update from decoupled weight decay. | Scalar weight `w=1.0`, gradient `g=0.1`, learning rate, betas, epsilon, weight decay. | Update first and second moments, bias-correct, apply adaptive step, then decoupled decay term. | Implementing L2 penalty inside the gradient and calling it AdamW; diagnostic is mismatch on a one-step hand calculation. | `assignments/ch07_training/`, DER-10, optimizer written problem | stable optimizer algorithm |
| WE-CH07-LRTRACE | Ch07 Training Loop / Scheduler | Trace warmup+cosine LR against optimizer steps and consumed tokens. | `base_lr=0.2`, warmup `2`, total steps `6`, `min_lr_ratio=0.25`, `tokens_per_step=1000`. | Step 0 is warmup with lr `0`; step 1 has multiplier `0.5` and lr `0.1`; step 2 starts cosine at multiplier `1.0` and lr `0.2`; step 6 reaches multiplier `0.25`, lr `0.05`, and `6000` consumed tokens. | Advancing the scheduler per micro-batch instead of per optimizer step; diagnostic is warmup/cosine finishing too early relative to consumed tokens. | `assignments/ch07_training/`, `lr_schedule_trace`, Ch07 scheduler written problem | optimizer-step schedule accounting |
| WE-CH07-STEPCYCLE | Ch07 Training Loop / Optimization Systems | Order gradient accumulation, AMP unscale, clipping, optimizer step, and scheduler step. | Four micro-batch mean losses `[2.4,2.0,1.8,2.2]`, `grad_accum_steps=4`, FP16 scaler, max grad norm `1.0`. | Divide each micro-batch loss by 4 before backward; scaled losses are `[0.6,0.5,0.45,0.55]`; after all backward passes, unscale gradients, clip global norm, run one optimizer step, update scaler, then advance scheduler once. | Stepping scheduler per micro-batch or clipping scaled gradients; diagnostic is warmup ending too early or clipping threshold applied in scaler units. | `assignments/ch07_training/`, Ch07 full train loop | training step ordering |
| WE-CH07-BUDGET | Ch07 Training Loop / Scaling | Compute global batch tokens, optimizer steps, and dense LM training FLOPs. | `micro_batch=4`, `seq_len=2048`, `grad_accum=8`, `data_parallel=16`, `D=20B`, `N=1B`. | Global batch tokens are `1,048,576`; step count is `ceil(20B / 1,048,576)=19,074`; dense LM training FLOPs are approximately `6ND=1.2e20`. | Reporting epochs without token budget or treating parameter count alone as training cost; diagnostic is missing step/FLOP calculation. | `assignments/ch07_training/`, Ch07 written budget problem | coarse scaling estimate |
| WE-CH07-OPTMEM | Ch07 Training Loop / Memory | Estimate AdamW training memory. | `N=1B`, bf16 parameters, bf16 gradients, fp32 AdamW moments. | Parameter bytes `2N`, gradient bytes `2N`, optimizer states `2*4N`; total is `12N` bytes before activation and sharding. | Counting only model weights during training; diagnostic is memory estimate missing gradients and moments. | `assignments/ch07_training/`, Ch07 optimizer memory problem | training memory accounting |
| WE-CH07-CKPTRESUME | Ch07 Training Loop / Checkpointing | Audit whether a distributed checkpoint can safely resume training. | Two checkpoints: one model-only export in `single_file` format with incomplete write and `12%` overhead; one older resume checkpoint missing RNG/sampler with non-monotonic step and `8%` overhead; expected world size `4`, max interval `200`, max overhead `5%`. | State completeness fails because optimizer/scheduler/RNG/sampler/scaler state is incomplete or export-only; write integrity fails because the latest write is incomplete and steps go backward; distributed reshard fails because single-file export is not sharded/DCP; interval and overhead gates fail, so action items are full resume state, atomic latest pointer, sharded/DCP checkpoint, shorter interval and async/overlapped save. | Treating a model export as a training checkpoint or ignoring optimizer/RNG/sampler state; diagnostic is resume loss/lr discontinuity, duplicated data shards, or inability to reshard after changing world size. | `checkpoint_resume_integrity_report`, Ch07 checkpoint written problem | distributed resume readiness gate |
| WE-CH08-TOPP | Ch08 Generation / Decoding | Compute a nucleus sampling candidate set and explain temperature effect. | Sorted probabilities `[0.40,0.25,0.20,0.10,0.05]`, `top_p=0.80`. | Accumulate until mass reaches threshold: keep first three tokens with mass `0.85`, renormalize before sampling. | Keeping tokens whose individual probability is below `p`; diagnostic is candidate set changes incorrectly when probabilities are sorted. | `assignments/ch08_generation/`, decoding quiz item | stable decoding procedure |
| WE-CH08-REPPENALTY | Ch08 Generation / Decoding | Apply repetition penalty before sampling. | Logits `[4.0,3.0,-1.0]`, generated ids `[0,2]`, penalty `2.0`. | Seen positive logit `4.0` becomes `2.0`; seen negative logit `-1.0` becomes `-2.0`; unseen token stays `3.0`, so greedy changes to token 1. | Penalizing after sampling or subtracting a constant from every seen token; diagnostic is missing the sign-dependent logits rule. | `assignments/ch08_generation/`, Ch08 decoding written problem | stable logits processor |
| WE-CH08-DISTTRACE | Ch08 Generation / Decoding | Trace the full sampling distribution after logits processors and top-p filtering. | Logits `[4.0,3.0,-1.0]`, generated ids `[0,2]`, penalty `2.0`, `top_p=0.8`. | Processed logits are `[2.0,3.0,-2.0]`; top-p keeps tokens `1` and `0`; renormalized probabilities are about `[0.269,0.731]` over tokens `[0,1]`; entropy is about `0.582`, and the highest-probability token is `1`. | Computing top-p before applying repetition penalty or reporting probabilities without renormalizing the kept set; diagnostic is token 2 keeps nonzero probability or probabilities do not sum to 1. | `assignments/ch08_generation/`, `decoding_distribution_report`, Ch08 written distribution problem | sampling probability accounting |
| WE-CH08-BEAM | Ch08 Generation / Search | Rank beam candidates with raw and length-normalized scores. | Candidates `a` with logprob `-0.2`, `a b c` with logprob `-0.8`, alpha `1.0`. | Raw score prefers `a`; normalized scores are `-0.2` and `-0.27`, showing why length penalty changes but does not solve quality. | Treating beam search as a quality metric; diagnostic is no discussion of task type or model probability bias. | `assignments/ch08_generation/`, written Ch08 beam problem | stable decoding search |
| WE-CH08-PASSK | Ch08 Generation / Evaluation | Estimate pass@k for code/math generation. | `n=10` sampled solutions, `c=2` correct, `k=3`. | `pass@3 = 1 - (8/10)*(7/9)*(6/8) = 0.5333`; this estimates at least one correct solution among 3 draws. | Reporting pass rate only for one sample; diagnostic is no distinction between accuracy@1 and pass@k. | `assignments/ch08_generation/`, reasoning generation written problem | stable benchmark metric |
| WE-CH08-SELFCONSIST | Ch08 Generation / Reasoning | Aggregate multiple reasoning samples by final-answer voting and token cost. | Outputs with extracted answers `[42,41,42]`, token counts `[12,15,18]`. | Majority answer is `42` with vote fraction `2/3`; total generated tokens are `45` and mean tokens per sample are `15`, so accuracy claims can be compared against test-time cost. | Reporting only the selected answer; diagnostic is no sample count, vote distribution, or token budget. | `assignments/ch08_generation/`, Ch08 reasoning written problem | test-time compute accounting |
| WE-CH08-TTCBUDGET | Ch08 Generation / Reasoning / Serving | Choose a reasoning strategy under quality, token, latency, and cost gates. | Greedy accuracy `0.55`, self-consistency-5 accuracy `0.72`, best-of-16 accuracy `0.78`; budgets cap output tokens and P95 latency. | The highest-accuracy strategy can fail token/latency gates; choose the best passing strategy, or require distillation/routing before serving. | Claiming best-of-N is better because benchmark score is highest; diagnostic is no marginal accuracy per extra token or latency gate. | `test_time_compute_budget_report`, Ch08 generation policy | reasoning budget gate |
| WE-CH08-CONSTRAINT | Ch08 Generation / Structured Output | Apply a token constraint mask before decoding. | Logits `[[5,4,3],[1,2,9]]`, allowed token ids `[[1,2],[0,1]]`. | Mask row 1 token 0 and row 2 token 2 to `-inf`; greedy over legal tokens returns `[[1],[1]]`; sampling must renormalize over the legal set. | Relying on prompt wording instead of masking illegal tokens; diagnostic is invalid JSON/tool-call tokens can still be sampled. | `assignments/ch08_generation/`, Ch08 constrained decoding problem | stable structured generation primitive |
| WE-CH08-SPEC | Ch08 Generation / Serving | Compute speculative decoding acceptance and rough speedup. | `gamma=4`, three rounds, accepted draft counts `[4,2,3]`, output tokens `[5,3,4]`, draft cost ratio `alpha=0.2`. | Proposed tokens `12`, accepted draft tokens `9`, generated tokens `12`, acceptance rate `75%`; baseline target calls `12`; speculative rough time is `3 + 12*0.2 = 5.4` target-step units, so speedup is about `2.22x`. | Treating `gamma=4` as guaranteed 4x speedup; diagnostic is no draft cost, acceptance rate, generated-token count, or target verify call count. | `assignments/ch08_generation/`, `speculative_decoding_speedup`, Ch08 speculative decoding exercise | serving acceleration accounting |
| WE-CH09-SFTMASK | Ch09 Fine-tuning / Alignment | Compute response-only SFT labels, shifted targets, and the valid-token loss denominator. | Chat tokens `[BOS,<user>,explain,BP,</user>,<assistant>,BP,computes,gradients,EOS]`; only assistant response tokens are supervised. | Prompt, role, and padding labels are `-100`; after shifting, valid targets are `[BP, computes, gradients, EOS]`, so the denominator is 4 rather than the sequence length. | Training on user prompt tokens or masking the first assistant token; diagnostic is the SFT loss denominator changes with prompt length or padding. | `assignments/ch09_alignment/`, Ch09 SFT labels problem | stable supervised fine-tuning objective |
| WE-CH09-SFTMASKPIPE | Ch09 Fine-tuning / SFT Data Pipeline | Audit multi-turn chat template, assistant-only masks, truncation, and packing risk before SFT. | Two tokenized conversations with `system/user/assistant` messages; one has an unknown role, one assistant answer is clipped by `max_seq_len`, and `pack_examples=True` without block-diagonal attention. | Report assistant spans, supervised tokens, supervised-token ratio, invalid roles, assistant-truncated tokens, and failed gates for `template_roles`, `assistant_label_mask`, `truncation`, and `packing`; action items point to role mapping, assistant mask, max length, or block-diagonal packing fixes. | Treating lower SFT loss as proof of correct instruction tuning when assistant tokens were truncated or packed examples can attend across boundaries. | `sft_chat_template_mask_report`, Ch09 9.3B, written Ch09 SFT pipeline problem | SFT data-protocol readiness gate |
| WE-CH09-RM | Ch09 Fine-tuning / Alignment | Compute Bradley-Terry reward-model loss and response-length bias statistics. | Chosen rewards `[3,1]`, rejected rewards `[1,2]`; chosen lengths `[10,8,5,7]`, rejected lengths `[6,8,9,4]`. | Reward margins are `[2,-1]`, so pairwise loss is mean `-logsigmoid`; preference accuracy is `1/2`. Length delta mean is `0.75`; chosen_longer_rate is `1/2`. | Treating higher reward accuracy as proof of better alignment; diagnostic is missing length-bias calculation. | `assignments/ch09_alignment/`, written Ch09 RM/bias problems | stable preference-model objective |
| WE-CH09-DATAGATE | Ch09 Fine-tuning / Alignment | Audit SFT/preference data before DPO/GRPO. | Five records with `kind`, `task`, `safety_slice`, chosen/rejected token counts, `winner_id`, `eval_overlap`, and `chosen_safety_violation`. | Count SFT vs preference records, task and safety-slice coverage, max task share, label conflicts from different winners on the same prompt, length-delta ratio, eval-overlap rate, and unsafe-chosen rate; fail the corresponding gate before optimizing. | Starting DPO/GRPO because records have chosen/rejected fields; diagnostic is no coverage, leakage, conflict, length-bias, or safety audit. | `post_training_data_audit`, Ch09 9.6A, written Ch09 data-gate problem | post-training data readiness gate |
| WE-CH09-DPO | Ch09 Fine-tuning / Alignment | Compute the sign of a DPO preference update from chosen/rejected log-prob ratios. | Chosen log-ratio `0.3`, rejected log-ratio `-0.1`, beta positive. | Preference margin is `0.4`; loss decreases as chosen improves relative to rejected, with reference model anchoring the ratio. | Comparing raw policy log-probs without reference ratios; diagnostic is reward hacking explanation missing the reference term. | `assignments/ch09_alignment/`, DER-12, paper recap DPO anchor | stable algorithmic objective |
| WE-CH09-DPOREWARD | Ch09 Fine-tuning / Alignment | Compute DPO implicit rewards and preference probability. | Policy chosen/rejected log-probs `[3,1]`, `[1,2]`; reference chosen/rejected log-probs `[1,1]`, `[1,1]`; `beta=0.5`. | Implicit chosen rewards are `[1,0]`, rejected rewards `[0,0.5]`; margins `[1,-0.5]`; preference probabilities are `sigmoid` of the margins and accuracy is `1/2`. | Treating raw policy log-prob as reward; diagnostic is the reference model disappears from the calculation. | `assignments/ch09_alignment/`, Ch09 DPO implicit reward problem | reference-anchored preference diagnostic |
| WE-CH09-PPO | Ch09 Fine-tuning / Alignment | Compute PPO clipped policy surrogate and update diagnostics. | Old log-probs all `0`; new/old ratios `[1.0,1.5,0.5]`; advantages `[1,1,-1]`; `clip_range=0.2`. | Unclipped surrogate is `[1.0,1.5,-0.5]`; clipped surrogate is `[1.0,1.2,-0.8]`; PPO uses elementwise min, so loss is `-mean([1.0,1.2,-0.8])`. Clip fraction is `2/3`; approx KL is mean `(ratio-1)-log(ratio)`. | Treating PPO clipping as a reward bonus or ignoring negative advantages; diagnostic is wrong sign on the third token. | `assignments/ch09_alignment/`, Ch09 PPO clipped objective problem | stable policy-gradient objective |
| WE-CH09-GRPOADV | Ch09 Fine-tuning / Alignment | Compute GRPO group-relative advantages per prompt. | Rewards matrix `[[1,10],[2,20],[3,30]]` with shape `[group=3,batch=2]`. | Whiten each prompt column separately using population std: both columns become approximately `[-1.225,0,1.225]`, so reward scale differs but relative ranking is preserved. | Normalizing across the whole batch or using default sample std without noticing the scale change; diagnostic is per-prompt advantage mean/std not `[0,1]`. | `assignments/ch09_alignment/`, Ch09 GRPO advantage test | group-relative reward scaling |
| WE-CH09-GRPOLOSS | Ch09 Fine-tuning / Alignment | Compute GRPO clipped policy loss with masked KL penalty. | Rewards `[[1],[2],[3]]`; new/old ratios `[[[1.0,1.5]],[[1.0,1.0]],[[0.5,1.0]]]`; mask `[[[1,1]],[[1,0]],[[1,1]]]`; ref log-probs all `0`; `clip_range=0.2`, `kl_beta=0.04`. | Advantages are about `[-1.225,0,1.225]`; valid-token clipped surrogates average to `-0.2449`, so policy loss is `0.2449`; masked approximate KL is about `0.0758`; total loss is about `0.2479`, with clip fraction `2/5`. | Applying one sequence reward without expanding it to completion tokens or averaging KL over masked padding; diagnostic is total loss changes when padding length changes. | `assignments/ch09_alignment/`, `grpo_policy_loss`, Ch09 GRPO policy-loss problem | alignment objective accounting |
| WE-CH09-KL | Ch09 Fine-tuning / Alignment | Compute masked approximate KL used to keep policy near reference. | Policy logps `[-0.2,-1.0,-3.0]`, ref logps `[-0.4,-0.7,-3.5]`, mask `[1,1,0]`. | Use `exp(log_ref-log_policy)-(log_ref-log_policy)-1` on valid tokens only, then average over two valid positions. | Averaging over padded tokens or using raw log-prob difference as KL; diagnostic is KL changes when sequence padding length changes. | `assignments/ch09_alignment/`, Ch09 GRPO/KL written problem | stable policy-regularization estimate |
| WE-CH10-KVCACHE | Ch10 Inference / RAG / Serving | Show why incremental decoding with KV cache avoids recomputing old keys/values. | Prompt length `T=4`, one new token, `n_layers`, `n_heads`, `d_head`. | Prefill computes KV for 4 tokens; decode step appends one KV row and attends one query over cached 5 positions. | Recomputing all KV tensors at every decode token; diagnostic is per-token latency growing like full-prefix forward cost. | `assignments/ch10_inference/`, benchmark/SLO reasoning | systems implementation detail |
| WE-CH10-PREFIXCACHE | Ch10 Inference / Serving | Estimate prefix cache prefill savings for a request stream. | Tokenized prompts `[[1,2,3,4],[1,2,3,9,10],[1,2,8],[7,8]]`. | Reusable prefix lengths are `[0,3,2,0]`; new prefill tokens are `[4,2,1,2]`; total prompt tokens `14`, saved prefill tokens `5`, effective prefill tokens `9`, hit rate `5/14`. | Adding random timestamps or request ids before the shared prompt; diagnostic is common prefix length collapses to zero. | `assignments/ch10_inference/`, Ch10 serving written problem | prefix-cache cost accounting |
| WE-CH10-ADMISSION | Ch10 Inference / Continuous Batching | Decide which requests enter one scheduler iteration. | Requests with prompt/max-new/cached-prefix/priority/wait fields; scheduler config has `max_num_seqs`, `max_num_batched_tokens`, `max_active_kv_tokens`, chunked prefill and queue SLO. | Sort by priority and waiting time, compute effective prefill tokens, scheduled chunk tokens and active KV reservation, admit while sequence/token/KV budgets pass, then report queued reasons and action items. | Treating continuous batching as unlimited batching; diagnostic is no `max_num_seqs`, token budget, active KV, chunked prefill, or queue wait gate. | `continuous_batching_admission_report`, Ch10 vLLM scheduler exercise | serving admission-control gate |
| WE-CH10-MCPGATE | Ch10 Inference / Tool Runtime Security | Audit a remote MCP-style tool event after schema validation. | Tool `filesystem_write` from untrusted remote server, risk `write`, permissions `read/write`, no user approval, roots outside allowed workspace, elicitation asks for unrelated secrets, sensitive data sent, unisolated prompt-injection observation, recursive sampling requested, 2 calls under policy max 1. | Schema may pass, but runtime gates fail: server trust, permission/consent, roots/elicitation, data privacy, observation isolation, sampling/budget. Action items are pin/remove server, request approval or downgrade risk, restrict roots, reject unsafe elicitation, redact/block egress, isolate observation, disable sampling, and stop/summarize loop. | Treating a valid JSON tool call as safe to execute; diagnostic is no server allowlist, consent, roots/elicitation scope, data-egress, untrusted observation, or recursive sampling gate. | `tool_runtime_security_report`, Ch10 MCP/tool security exercise | agent runtime safety gate |
| WE-CH10-ROLLOUT | Ch10 Inference / Production Release | Decide whether a model/config candidate can expand beyond canary. | Baseline pass/safety/cost; candidate pass/safety/P95/error/cost/canary traffic/sample/control comparison/rollback/monitors; policy thresholds. | Check offline quality regression, safety regression, SLO, cost regression, canary size/traffic/control, and rollback plus per-version monitoring. Passing all gates promotes to the next stage; safety, canary, or rollback failures block release. | Shipping an offline eval win directly to full traffic; diagnostic is no control group, canary sample, per-version quality/safety/SLO/cost monitors, or rollback readiness. | `production_rollout_gate_report`, Ch10 production readiness exercise | release safety gate |
| WE-CH10-OVERLOAD | Ch10 Inference / Production Operations | Diagnose serving overload and choose mitigation. | Metrics: waiting requests `80`, queue P95 `900 ms`, TTFT `1800 ms`, TPOT `95 ms`, KV usage `0.96`, swapped requests `3`, error `0.03`, timeout `0.02`; tenant `free-tier` at `40 rps` over quota `10`. | Queue, KV, decode, error budget, tenant fairness, and degradation readiness gates fail. Decision is `load_shed_and_page_owner`; actions include tenant quota enforcement, low-priority shedding, context/KV reduction, max-token/decode capacity mitigation, and incident response. | Waiting for 5xx before acting or blaming the model when a noisy tenant and KV swapping are the root cause; diagnostic is no queue/KV/decode/quota separation. | `serving_overload_response_report`, Ch10 overload runbook exercise | runtime reliability gate |
| WE-CH10-PDPOOL | Ch10 Inference / Disaggregated Serving | Size prefill/decode pools and KV transfer links for a workload. | `20 QPS`, mean prompt `2000`, output `200`, prefix hit rate `0.25`; two prefill workers at `30k tok/s`, two decode workers at `2.5k tok/s`, one KV link at `50k tok/s`, target utilization `0.8`. | Effective prefill and KV transfer load are `20*2000*0.75=30k tok/s`; decode load is `4k tok/s`; required workers are prefill `2`, decode `2`, transfer link `1`. Then check active KV tokens against decode memory capacity before approving P/D disaggregation. | Treating P/D as automatically faster once prefill is split out; diagnostic is no KV transfer or active KV memory gate. | `pd_pool_capacity_plan`, P/D serving table | disaggregated serving capacity gate |
| WE-CH10-SPECGATE | Ch10 Inference / Speculative Serving | Decide whether speculative decoding should be enabled for a measured workload. | Baseline `2000 ms`, speculative `940 ms`, output `200`, draft `200`, accepted `152`, verify steps `50`, draft cost `200 ms`, memory overhead `1.5 GB`, QPS `5`; gates require acceptance `>=0.7`, speedup `>=1.8`, draft overhead `<=0.25`. | Acceptance is `152/200=0.76`; speedup is `2000/940=2.13x`; tokens per verify step is `4`; draft overhead is `200/940=0.213`; if quality regression and memory gates pass, enable speculation for this workload. | Enabling speculative decoding because an engine supports EAGLE/MTP or because `gamma=4`; diagnostic is no acceptance, speedup, draft-cost, quality, memory, or QPS gate. | `speculative_serving_gate_report`, Ch10 serving written problem | speculative decoding production gate |
| WE-CH10-LONGCTXGATE | Ch10 Inference / Long Context Serving | Gate a long-context serving path beyond max context length. | Three records at head/middle/tail needle positions, context `32K/64K/96K`, max context `128K`, answer/citation hits, P95 TTFT, latency, KV usage and prefix hit rate. | Passing gate requires no truncation, high answer and citation recall, coverage across head/middle/tail with acceptable bucket accuracy, and serving signals under SLO. If tail recall fails while TTFT/KV are high, the action is not just "increase context"; add position/distractor eval, context packing/RAG, prefix cache/chunked prefill or stricter long-context admission. | Claiming 128K/1M context support proves the product can answer long-document questions; diagnostic is no truncation audit, no needle position sweep, no citation recall, and no KV/TTFT budget. | `long_context_serving_gate_report`, long-context serving table | long-context quality and cost gate |
| WE-CH10-INFONCE | Ch10 Inference / RAG / Retrieval Training | Compute in-batch contrastive retrieval logits. | Normalized similarity matrix `[[0.8,0.2],[0.1,0.7]]`, temperature `0.5`, diagonal positives. | Divide by temperature to get logits `[[1.6,0.4],[0.2,1.4]]`; labels are `[0,1]`; each row's positive probability is about `0.768`, so loss is about `0.264`. | Treating every off-diagonal document as a safe negative or treating cosine similarity as fixed magic; diagnostic is no training objective connecting query/doc positives to vector space geometry. | `assignments/ch10_inference/`, Ch10 RAG worked example | stable contrastive retrieval objective |
| WE-CH10-RERANKLOSS | Ch10 Inference / RAG / Reranker Training | Compute pairwise reranker loss. | Chosen scores `[3.0,1.0]`, rejected scores `[1.0,1.5]`. | Margins are `[2.0,-0.5]`; loss is mean `softplus(-margin)`, and preference accuracy is `1/2`. | Training only the retriever and assuming rerank is fixed; diagnostic is no objective for top-k ordering errors. | `assignments/ch10_inference/`, Ch10 RAG written problem | stable pairwise ranking objective |
| WE-CH10-RAG | Ch10 Inference / RAG / Serving | Compute Recall@k and reciprocal rank for a retrieved document list. | Retrieved ids `[doc9, doc2, doc5, doc1]`, relevant ids `{doc1, doc2, doc7}`, `k=3`. | Top-3 contains one of three relevant docs, so Recall@3 is `1/3`; first relevant doc is rank 2, so reciprocal rank is `1/2`. | Judging RAG only from generated answer text; diagnostic is no separate retrieval metric. | `assignments/ch10_inference/`, Ch10 RAG written problem | stable retrieval metric |
| WE-CH10-NDCG | Ch10 Inference / RAG / Serving | Compute nDCG@k with graded relevance. | Retrieved ids `[doc3, doc2, doc1]`, relevance `{doc1:3, doc2:2, doc3:1}`, `k=3`. | DCG is `1/log2(2) + 3/log2(3) + 7/log2(4)`; IDCG reverses the order, so nDCG is about `0.68`. | Treating all relevant chunks as equal; diagnostic is a system that recalls the right set but ranks the most useful chunk last. | `assignments/ch10_inference/`, Ch10 RAG written problem | stable graded retrieval metric |
| WE-CH10-RERANK | Ch10 Inference / RAG / Serving | Fuse dense/BM25 rankings and rerank candidates. | Dense rank `[A,B,C]`, sparse rank `[B,D,A]`, cross-encoder scores for top candidates. | RRF adds `1/(k+rank)` from each list, often lifting docs that rank well in both; rerank then sorts candidates by query-document score before prompt assembly. | Treating rerank as recall improvement instead of ordering improvement; diagnostic is Recall unchanged but reciprocal rank/top-1 relevance changes. | `assignments/ch10_inference/`, Ch10 RAG written problem | stable retrieval pipeline |
| WE-CH10-MMR | Ch10 Inference / RAG / Serving | Select non-redundant chunks with maximal marginal relevance. | Query similarities `[1.0,0.99,0.45]`; doc 0 and doc 1 are near duplicates; lambda `0.5`, `top_k=2`. | Pick doc 0 first; for the second item compare query relevance minus redundancy to selected docs, so a less similar but diverse doc can beat the duplicate. | Taking top-k nearest chunks and filling the prompt with repeats; diagnostic is high Recall but duplicated context and missed complementary context. | `assignments/ch10_inference/`, Ch10 RAG written problem | stable retrieval diversification |
| WE-CH10-CONTEXTPACK | Ch10 Inference / RAG / Serving | Pack retrieved chunks with citations under a prompt budget. | Chunks `A:2 words`, `B:3 words`, `C:4 words`; citation marker adds one token; max context tokens `9`, reserved output tokens `2`. | Context budget is `7`; chunk A costs `3`, B costs `4`, C would exceed the remaining budget, so citations `[A,B]` enter the prompt, used tokens are `7`, and skipped chunks are `1`. | Reporting Recall@k without checking whether relevant chunks fit into the prompt; diagnostic is relevant citation retrieved but absent from final context. | `assignments/ch10_inference/`, Ch10 prompt assembly written problem | context-budget accounting |
| WE-CH10-RAGDIAG | Ch10 Inference / RAG / Evaluation | Attribute an incorrect RAG answer to retrieval, context/citation, or generation. | Retrieved ids `[doc9,doc2,doc5,doc1]`, relevant ids `{doc1,doc2,doc7}`, cited ids `[doc2,doc9]`, `k=3`, answer incorrect. | Recall@3 is `1/3`, MRR@3 is `1/2`; citation precision is `1/2`, citation recall is `1/3`; relevant docs `doc1` and `doc7` are missing from top-3, but one relevant doc was cited, so the failure mode is generation error rather than pure retrieval miss. | Treating every wrong answer as hallucination or every low recall as generation failure; diagnostic is no separate retrieval/citation/generation attribution. | `assignments/ch10_inference/`, `rag_answer_diagnostics`, Ch10 RAG failure problem | RAG subsystem attribution |
| WE-CH10-STRUCTGATE | Ch10 Inference / Structured Output | Gate JSON/schema constrained output before release. | Three records: one invalid JSON, one valid JSON with enum drift, one valid JSON with extra field and high latency; policy requires JSON/schema valid rate `>=0.9`, retry rate `<=0.2`, P95 latency `<=500 ms`, fallback and safety violation rate `0`. | JSON valid rate is `2/3`; schema valid rate is `0/3`; retry/fallback/safety gates fail, so the action items are to enable JSON/constrained decoding, strict schema repair, reduce retries, and block release until safety passes. | Treating parseable JSON as sufficient for production; diagnostic is no schema adherence, retry latency, fallback/refusal, or safety gate. | `structured_output_reliability_report`, Ch10 tool/serving written problem | structured-output release gate |
| WE-CH11-RNN | Ch11 Classic NLP / RNN Foundations | Compute scalar RNN states and BPTT gradient factors. | `x=[1.0,0.5,-1.0]`, `w_x=0.8`, `w_h=0.4`, `h0=0`. | Recurrence is `h_t=tanh(w_x*x_t+w_h*h_{t-1})`; local gradient factor is `w_h*(1-h_t^2)`. | Treating the final hidden state as if it directly stores all early tokens equally; diagnostic is missing the product of recurrent factors. | `assignments/ch11_classic_nlp/`, classic NLP handout, written problem set | stable sequence-model math |
| WE-CH11-PARSE | Ch11 Classic NLP / Dependency Parsing | Execute an arc-standard transition sequence. | Tokens `I saw her`; actions `SHIFT, SHIFT, LEFT_ARC(nsubj), SHIFT, RIGHT_ARC(obj), ROOT(root)`. | Heads become `[1,-1,1]`; labels are `[nsubj,root,obj]`; arcs are `saw->I`, `saw->her`, `ROOT->saw`. | Reversing LEFT/RIGHT arc head direction; diagnostic is UAS/LAS mismatch on the same gold tree. | `assignments/ch11_classic_nlp/`, classic NLP handout, written problem set | stable structured prediction |
| WE-CH11-S2S | Ch11 Classic NLP / Seq2Seq Attention | Compute additive-attention alignment and context. | Decoder state `s_t`, three encoder states, small `W_s`, `W_h`, and `v`. | Compute `v^T tanh(W_s s_t + W_h h_i)` for each source position, softmax over source positions, then weighted-sum encoder states into `c_t`. | Treating cross-attention K/V as if they came from generated target prefix; diagnostic is context vector not tied to source states. | `assignments/ch11_classic_nlp/`, seq2seq written problem | stable encoder-decoder attention math |
| WE-CH11-MLM | Ch11 Classic NLP / Encoder-only | Compute BERT-style MLM loss only on masked positions. | Three positions, vocab logits, labels `[None,2,0]`. | Ignore position 0; compute cross entropy for positions 1 and 2 only, average over two masked labels, and compute accuracy from argmax at those two positions. | Averaging loss over every token in the sequence; diagnostic is unmasked tokens changing the denominator or accuracy. | `assignments/ch11_classic_nlp/`, BERT MLM written problem | stable encoder pretraining objective |
| WE-CH11-MLMQA | Ch11 Classic NLP / Encoder-only | Compare MLM loss masking with extractive QA span selection. | Masked input `[CLS] the [MASK] [MASK] [SEP]`; QA input `[CLS] question [SEP] passage [SEP]`; start/end logits. | MLM averages CE over only two masked positions and reports masked-token accuracy; QA scores legal spans by `start_logit + end_logit` and compares the best span with `[CLS]` no-answer. | Treating BERT as a free-form generator or averaging MLM loss over unmasked tokens; diagnostic is outputs not tied to mask positions or input spans. | `assignments/ch11_classic_nlp/`, Ch11 encoder-only written problem | stable encoder objective and head design |
| WE-CH11-BIO | Ch11 Classic NLP / Sequence Labeling | Decode BIO tags into entity spans. | Tokens `[John, works, at, OpenAI]`, tags `[B-PER, O, O, B-ORG]`. | Output PER span `John` at `[0,1)` and ORG span `OpenAI` at `[3,4)`; adjacent `I-TYPE` tokens extend the current span. | Treating token labels as independent final answers; diagnostic is no span-level entity output for NER or slot filling. | `assignments/ch11_classic_nlp/`, BERT/token classification written problem | stable sequence-labeling representation |
| WE-CH11-SPANF1 | Ch11 Classic NLP / Sequence Labeling | Compute exact span-level precision, recall, and F1. | Gold spans `PER[0,1)`, `LOC[3,5)`; predicted spans `PER[0,1)`, `LOC[3,4)`, `ORG[6,7)`. | Only `PER[0,1)` matches exactly, so TP=1, FP=2, FN=1, precision `1/3`, recall `1/2`, F1 `0.4`. | Reporting token accuracy for NER; diagnostic is a boundary error counted as mostly correct tokens but wrong entity span. | `assignments/ch11_classic_nlp/`, BERT/token classification written problem | entity-level evaluation |
| WE-CH11-VITERBI | Ch11 Classic NLP / Structured Decoding | Decode the best tag path with emission and transition scores. | Tags `O,B-ORG,I-ORG`; emissions prefer `B-ORG` then weakly prefer `O`, transition strongly rewards `B-ORG -> I-ORG`. | Viterbi chooses `[B-ORG,I-ORG]` because global path score includes transition terms; local argmax would miss this dependency. | Taking per-token argmax independently; diagnostic is BIO-invalid or low-transition-score paths. | `assignments/ch11_classic_nlp/`, token classification written problem | stable dynamic-programming decoder |
| WE-CH11-CRF | Ch11 Classic NLP / Structured Decoding | Compute the CRF log-partition and negative log likelihood. | Two tags, three positions, emission table, transition table, and gold tags. | Replace Viterbi `max` with `logsumexp` to get `log Z`; separately score the gold path; CRF NLL is `log Z - gold_score`. | Training from only the best competing path; diagnostic is using Viterbi score as the normalization term or omitting the gold path score. | `assignments/ch11_classic_nlp/`, token classification written problem | stable CRF training objective |
| WE-CH11-BERTQA | Ch11 Classic NLP / Encoder-only | Select an extractive QA span from start/end logits. | Tokens `[CLS] the quick brown fox [SEP]`; start high at `quick`, end high at `brown`. | Score each legal span by `start_logit + end_logit`; choose `quick brown`; return no-answer if `[CLS]` score is higher. | Treating BERT QA as free-form generation; diagnostic is answer span not tied to input token positions. | `assignments/ch11_classic_nlp/`, BERT written problem | stable encoder fine-tuning pattern |
| WE-CH11-METRICS | Ch11 Classic NLP / Evaluation | Compute dependency UAS/LAS and explain why metric choice changes conclusions. | Three-token sentence with gold heads/labels and predicted heads/labels. | UAS counts matching heads; LAS counts both head and dependency label; one wrong label can keep UAS correct but lower LAS. | Reporting only one metric as overall quality; diagnostic is hidden label errors in high-UAS output. | `assignments/ch11_classic_nlp/`, classic NLP handout, written problem set | stable evaluation definition |
| WE-CH11-QAF1 | Ch11 Classic NLP / Evaluation | Compute normalized exact match and token F1 for extractive QA. | Prediction `quick fox`; gold answer `quick brown fox`; normalization lowercases and removes punctuation/articles. | EM is `0`; overlap tokens are `quick` and `fox`, so precision `2/2`, recall `2/3`, and F1 `0.8`; with prediction `The quick brown fox`, EM and F1 are both `1`. | Reporting only exact string match or using unnormalized punctuation/articles; diagnostic is correct partial answers scored as zero without token F1. | `assignments/ch11_classic_nlp/`, QA metric tests | extractive-QA evaluation |
| WE-CH11-JUDGEAUDIT | Ch11 Modern LLM Evaluation | Audit whether an LLM judge can support a model comparison. | Pairwise records for `q1` in both A/B orders, response ids, token lengths, and human `gold_winner`. | Convert A/B winner to chosen response id, check swapped-order consistency, first-position preference rate, longer-answer win rate, and gold agreement before trusting tie-adjusted win rate. | Reporting a single LLM-as-judge win rate while the judge flips under swapped order or prefers longer answers; diagnostic is no reliability gate. | `judge_reliability_audit`, Ch11 evaluation written problem | judge reliability gate |
| WE-X-END2END | Cross-Chapter LLM Pipeline | Trace one decoder-only batch from token ids to loss and serving cost. | `input_ids` shape `[2,5]`, vocab `8`, `d_model=4`, `H=2`, labels contain three `-100` positions; fp16 KV cache, `layers=4`, `H_kv=2`, `d_head=2`. | Embeddings `[2,5,4]`; Q/K/V `[2,2,5,2]`; scores `[2,2,5,5]`; logits `[2,5,8]`; shifted labels have `2*(5-1)=8` positions minus ignored shifted labels. KV cache bytes are `2*B*layers*T*H_kv*d_head*2`. | Keeping formulas isolated by chapter; diagnostic is loss denominator, attention score shape, or KV cache budget computed with inconsistent symbols. | written cross-chapter comprehensive problem | course-level synthesis |
| WE-X-RAGGEN | Cross-Chapter RAG / Generation / Evaluation | Diagnose a RAG answer from retrieval ranking through generation metrics. | Dense rank `[A,B,C]`, BM25 rank `[B,D,A]`, reranker scores, chunk token costs, context budget, generated answer and gold answer. | Use RRF to fuse candidates, rerank by cross-encoder score, pack citations under budget, compute Recall/MRR on retrieved ids and EM/F1 on answer text; separate retrieval miss from generation hallucination. | Judging the whole system from answer text only; diagnostic is no attribution to retrieval, reranking, context packing, or generation. | written cross-chapter comprehensive problem | system-level evaluation |
| WE-X-ALIGN-SERVE | Cross-Chapter Alignment / Serving | Compare an aligned model update against latency and safety constraints. | Policy/ref log-probs for chosen/rejected responses, refusal labels, output token counts, TTFT/TPOT table. | Compute DPO margin and preference probability, approximate KL on sampled tokens, refusal/over-refusal rates, and token cost; decide whether the update improves target behavior without exceeding latency or safety constraints. | Treating preference win rate as the only alignment metric; diagnostic is no reference KL, refusal breakdown, or serving cost. | written cross-chapter comprehensive problem | objective-to-system tradeoff |

## Recitation Use

| recitation_id | examples | activity | exit learning output |
| --- | --- | --- | --- |
| WE-R1-SHAPE | WE-CH02-ROPE, WE-CH03-ATTN | Students fill shape tables and identify where the mask or rotation enters. | one corrected shape trace |
| WE-R2-SYSTEMS | WE-CH04-GQA, WE-CH10-KVCACHE | Students compute memory ratios and explain latency consequences. | one memory/latency calculation with units |
| WE-R3-OBJECTIVES | WE-CH07-ADAMW, WE-CH09-DPO | Students compare optimizer and preference-objective failure modes. | one signed update explanation |
| WE-R4-EVAL | WE-CH08-TOPP, WE-CH11-METRICS | Students explain why generation and evaluation choices change observed quality. | one metric or sampling boundary note |
